// src/shared/ports/documentos.port.js

/**
 * Contrato formal do serviço de documentos.
 * Módulos não devem importar services diretamente.
 */

export const DocumentosPort = {
  obterPorToken: null,
  emitirDocumentoAssinadoExterno: null,
  substituir: null,
};
