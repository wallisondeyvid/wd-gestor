// (migrado) Rotas de login - views agora centralizadas em pagesRouter
import express from 'express';
const router = express.Router();
// A view /login é servida por pagesRouter. Mantido router vazio para compatibilidade.
export default router;
