// (migrado) Rotas de módulos
import express from 'express';
// Este arquivo mantém apenas rotas específicas futuras (se necessário).
// As views de /modulos são servidas por pagesRouter com requireLogin.
const router = express.Router();
export default router;
