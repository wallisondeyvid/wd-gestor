// (migrado) Rotas de setores
import express from 'express';
// A view /setores é servida por pagesRouter com requireLogin; este arquivo fica reservado para rotas extras no futuro.
const router = express.Router();
export default router;
