## 2026-04-07 — fechamento da frente

Rodada concluída e publicada em `origin/migration/refactor-core`.

Estado final verificado:
- working tree clean
- branch sincronizada com origin
- sem diff pendente contra remoto

Topo final da sequência:
- `7d33376` test(architecture): autoriza fachadas oficiais de create core e unidade usecase
- `a484f67` refactor(gestor): adiciona fachada de unidade controller usecase
- `79512d5` refactor(gestor): adiciona fachada de funcionario create core

Observação:
- suíte global validada antes do push
- frente encerrada sem pendências locais