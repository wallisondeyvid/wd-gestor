name: Migration Safety

on:
  pull_request:
  push:
    branches:
      - main
      - migration/**
      - migration-**
      - refactor/**

jobs:
  safety:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install deps
        run: npm ci

      - name: Guard migration
        run: npm run guard:migration

      - name: Run tests
        run: npm test
